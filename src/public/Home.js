import React from 'react';
import PageContainer from './PageContainer';

// Home page component
const Home = () => (
  <div id="home">
    <PageContainer>
      <h1>Marvin</h1>
    </PageContainer>
  </div>
);

export default (Home);
