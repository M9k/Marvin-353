import React from 'react';
import PageContainer from './PageContainer';

// Home page component
const Home = () => (
  <div id="home">
    <PageContainer>
      HOME
    </PageContainer>
  </div>
);

export default (Home);
